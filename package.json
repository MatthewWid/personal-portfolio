{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "repository": "git@github.com:MatthewWid/personal-portfolio.git",
  "license": "MIT",
  "scripts": {
    "sass": "sass ./src/styles/:./public/css/",
    "sass:watch": "sass --sourcemap=none --watch ./src/styles/:./public/css/",
    "watch": "nodemon ./src/server.js",
    "start": "npm run sass && node ./src/server.js",
    "dev": "concurrently \"npm run sass:watch\" \"npm run watch\""
  },
  "dependencies": {
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "pug": "^2.0.3",
    "serve-favicon": "^2.5.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.9"
  }
}
